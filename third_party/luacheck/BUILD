# luacheck (see VERSION), MIT. Pure Lua; the part its API needs is embedded as
# bytecode by //src/kernel/lua.
filegroup(
    name = "sources",
    srcs = glob(["src/**/*.lua"]),
    visibility = ["//visibility:public"],
)
